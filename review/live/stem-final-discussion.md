◈ Failed action

`record result` on turn 2 failed. The game replied: "Conditions are not ready."

◈ Its prerequisite

`record result` requires the flag `red_tested`. The player had no flags at that point. To set `red_tested`, the procedure requires:

1. Take the prism and screen from the storeroom.
2. `open shutter` — sets `beam`.
3. `use prism` — requires `beam`; sets `spectrum`.
4. `use screen` — requires `spectrum`; sets `red_selected`.
5. `test red light` — requires `red_selected` and `beam`; sets `red_tested`.

Only then does `record result` pass. The record shows the player went straight to the laboratory and attempted the final step with an empty inventory and no flags set.

◈ Scripted observations vs. historical evidence

Everything quoted in this record is scripted game text, not a measurement or a source. That includes "an elongated band of colours appears," "selected red light remains red," and "sunlight formed a spectrum." These are simplified, designed outputs for play.

The record contains no historical evidence. It only says to compare the result with Newton's account. Historical evidence would come from the sources themselves — for example, the Newton: Light and Colour entry in the knowledge set. That material can support or complicate the game's simplification, but it is separate from the game's scripted observations.
